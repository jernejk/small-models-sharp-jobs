# Retired pre-restructure material

These documents and checkpoint snippets describe the removed root `starter/` and `solution/` layout.
They are retained only as historical facilitator research. They are not an attendee path and must not
be used for workshop delivery; use the numbered projects in `workshop/` and the reference at
`facilitator/reference/solution/` instead.
