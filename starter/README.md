# Starter — Small Models, Sharp Jobs

Generated from the canonical `src/` tree. Do not edit this file by hand.

You will complete 4 TODOs. The project compiles now and throws
`NotImplementedException` at each one until you fill it in.

- **TODO 1** — `src/Workshop.Core/EvidenceStore.cs`
- **TODO 2** — `src/Workshop.App/IncidentPipeline.cs`
- **TODO 3** — `src/Workshop.App/IncidentPipeline.cs`
- **TODO 4** — `src/Workshop.Core/Verifier.cs`

## Loop

```bash
dotnet test                                        # red until the TODOs are done
dotnet run --project src/Workshop.App -- run       # writes the three artifacts
```

Stuck? The finished code for every TODO is in `../solution/`.
