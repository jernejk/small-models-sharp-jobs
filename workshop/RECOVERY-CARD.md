# Recovery card

After five minutes of setup, pair with a working participant or use `solution/` for the checkpoint. Without a model, run `dotnet test` and the supported and no-result Gather commands. A compatible hosted endpoint is participant-owned and optional; use only placeholder environment variables and never share a facilitator key.

The corpus is a de-identified, attributed Victorian crash subset for analyst training, not claims automation.
